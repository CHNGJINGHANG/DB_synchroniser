services:
  - type: web
    name: sync-clicker-trainer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --threads 8 --timeout 120 sync_clicker_trainer:app
    plan: free
